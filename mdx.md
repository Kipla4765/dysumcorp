upload all file types types - drive, dropbox

check if are listed well on the assets page

delete from app and check on -dropbox, -drive

delete from drive and dropbox and check on app 

upload large file video 1gb+ to both

## to fix

- on create&edit portal storage section creating folder redirect out of page on finish
- create and edit page in storage section .it gives message recconect storage and redirects to storage page where another problem , one needs to enter the page for it to refresh the storage session token. 2 probs storage page not refreshing token automatically on expirery or ever so of backgroung job and create&edit portal page storage section depending on this
- create and edit portal page - remove view portal button on the last page
- create and edit portal page foldering section - google drive and dropbox foldering are not the same. unify implementing the best of the 2.     on the same remove the default folder path section and implement in in the navigation tree section ie this section in the on for drive:

**Navigation TreeNew Folder**

**My Drive**

it should be though:

**Navigation TreeNew Folder**

Dysumcorp / portal name / clientname(if selected)

- on the create&edit portal page a user should not have to open the storage section for the storage and folders to load . all things of such should load on entry to the page in general itself ie: the create and the edit portal page first entry page

## upload page professional ordering and design

- implement a default color theme on portal creation setting that is suitable and proffesional in the resulting portal
- implement color themes that are proffesional
- add on the create and edit portal page as well as complete implementation to the